// Uma imagem com dimensões 55 pixels possui níveis de cinza expressos por:
// f (x, y) = |x − y| , para x, y = 0,1,2,3,4. Encontre uma imagem de saída resultante da
// aplicação de um filtro da média utilizando uma janela em forma de cruz, isto é,
// considerando no cálculo da média apenas os pixels de coordenadas (x,y), (x-1,y), (x+1,y),
// (x, y-1) e (x,y+1).
